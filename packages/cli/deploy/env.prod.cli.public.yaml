RELEASE_BRANCH: prod
HEADLESS_ACCOUNT_EMAIL: cronjob_headless_account
GRAPHQL_ENDPOINT: https://prod-congress-dashboard-cms-505721902139.asia-east1.run.app/api/graphql
# Inferred by matching packages/frontend/.env.release.public's
# NEXT_PUBLIC_ALGOLIA_APP_ID, on the assumption cli writes to the same
# Algolia app frontend searches. Not independently verified — double check
# before relying on it.
ALGOLIA_APP_ID: 997QIW708J
# Job behavior is controlled by the container's --args (not managed by this
# file) or by adding LAWMAKER_* env vars here — see packages/cli/README.md
# for the available variables.
