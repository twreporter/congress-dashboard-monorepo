# Draft — review before first deploy.
RELEASE_BRANCH: dev
HEADLESS_ACCOUNT_EMAIL: cronjob_headless_account
GRAPHQL_ENDPOINT: https://dev-congress-dashboard-cms-505721902139.asia-east1.run.app/api/graphql
ALGOLIA_APP_ID: HX3S1GJ1QI
# Job behavior is controlled by the container's --args (not managed by this
# file, and defaults to the image's Dockerfile CMD if unset) or by adding
# LAWMAKER_* env vars here — see packages/cli/README.md for the available
# variables. The Dockerfile CMD defaults to a dry run.
