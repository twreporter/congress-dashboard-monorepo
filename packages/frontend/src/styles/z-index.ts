export enum ZIndex {
  Tooltip = 500,
  ControlBar = 900,
  Header = 1000,
  SearchBarInBody = Header - 1,
  Bar = SearchBarInBody - 1,
  HamburgerMenu = 1100,
  FilterModal = 1200,
  SideBar = 3000,
  Feedback = 5000,
}
